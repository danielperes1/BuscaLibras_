# README

- Clona o projeto
- Crie o banco com o script da pasta data
- Abra o terminal, entre na pasta server
- `npm i` para instalar os pacotes necessários
- Crie o arquivo `.env` com os seguintes dados

```env
PORT=3000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=usbw
DB_NAME=sistema_buscalibras

JWT_SECRET='cr7'
